# Copy to .env and fill in before running `docker compose up`.
TYPSTIFY_SERVER_PASSWORD=change-me

# Domain the `caddy` service issues its Let's Encrypt certificate for. Point
# this domain's DNS A record at the VPS's IP before starting the stack.
TYPSTIFY_DOMAIN=your-domain.example.com
